{
  "//": "Deno config for Supabase Edge Functions. Its presence (plus .vscode/settings.json deno.enablePaths) tells the Deno language server to own this folder, so `Deno`, std, and https:// imports resolve instead of showing as TS errors. The rest of the monorepo stays on the Node/TypeScript language server.",
  "imports": {
    "@supabase/supabase-js": "https://esm.sh/@supabase/supabase-js@2",
    "std/": "https://deno.land/std@0.224.0/"
  },
  "lint": {
    "rules": { "tags": ["recommended"] }
  },
  "fmt": {
    "lineWidth": 100,
    "singleQuote": true,
    "semiColons": true
  }
}
