{
  "$schema": "https://turbo.build/schema.json",
  "//": "envMode loose passes the full environment through to tasks. Turbo 2.x defaults to strict, which on Windows strips system vars (SystemRoot, ComSpec, …) that node/tsc need, making every task instantly exit 1 with no output.",
  "envMode": "loose",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    }
  }
}
